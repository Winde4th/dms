<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8" />
        <meta http-equiv="X-UA-COMPATIBLE" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Drupal Monitoring System | {% block title %}Home{% endblock %}</title>

        <!-- HTML5 Shim and Respond.js add IE8 support of HTML5 elements and media queries -->
        {% include 'BraincraftedBootstrapBundle::ie8-support.html.twig' %}
        <link href="{{ asset('css/bootstrap.css') }}" rel="stylesheet" media="screen">
        <link rel="stylesheet" href="{{ asset('css/styles.css') }}">
        {% block stylesheets %}{% endblock %}
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
    </head>
    <body>
        <nav class="navbar navbar-default" role="navigation">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-ex1-collapse">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <a class="navbar-brand" href="{{ path('actency_dms_homepage') }}">Home</a>
            </div>

            <div class="collapse navbar-collapse navbar-ex1-collapse">
                {{ knp_menu_render('ActencyDMSBundle:Builder:leftNavbarMenu', { 'style': 'navbar' }) }}
                {{ knp_menu_render('ActencyDMSBundle:Builder:rightNavbarMenu', { 'style': 'navbar-right' }) }}
                <form class="navbar-form navbar-right" role="search">
                    <div class="form-group">
                        <input type="text" class="form-control" placeholder="Search">
                    </div>
                    <button type="submit" class="btn btn-default">Submit</button>
                </form>
            </div>
        </nav>

        <div class="container-fluid">
            <div class="row-fluid">

                <h1 id="page-title">{% block page_title %}Home{% endblock %}</h1>

                <div id="flash-messages">{% include 'BraincraftedBootstrapBundle::flash.html.twig' with { 'close': true } %}</div>

                {% block blockquote %}
                    <blockquote class="blockquote-reverse">
                        <p>Nos défauts devraient nous donner une qualité: l'indulgence pour les défauts des autres.</p>
                        <footer><cite title="Source">Rivarol</cite></footer>
                    </blockquote>
                {% endblock %}

                <div id="sidebar" class="col-md-2">
                    <h2>{% block sidebar_title %}{% endblock %}</h2>
                    {% block sidebar %}{% endblock %}
                </div>
                <div id="content" class="col-md-10">
                    <h2>{% block content_title %}{% endblock %}</h2>
                    {% block content %}{% endblock %}
                </div>
            </div>

            <footer>
                <p class="text-center">&copy; {{ 'now'|date('Y') }} | Actency</p>
            </footer>

        </div><!-- /container -->
        <!-- jQuery (necessary for Bootstraps JavaScript plugins) -->
        <script src="{{ asset('js/jquery.js') }}"></script>
        <!-- Include all JavaScripts, compiled by Assetic -->
        <script src="{{ asset('js/bootstrap.js') }}"></script>
        {% block javascripts %}
            <script type="text/javascript">
                jQuery(function () {
                    jQuery('.tt').tooltip({'placement': 'bottom'});
                    jQuery('.pp').popover({'placement': 'bottom', 'trigger': 'hover'});
                });
            </script>
        {% endblock %}
    </body>
</html>
